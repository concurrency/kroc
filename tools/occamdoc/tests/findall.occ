--** Likely definitions without occamdoc.

PROC TYPE aaa IS (CHAN INT in?, out!):

INT REC FUNCTION bbb (VAL INT x) IS x:
INT RECURSIVE FUNCTION ccc (VAL INT x) IS x:
INT FUNCTION ddd (VAL INT x) IS x:
INT RECURSIVE INLINE FUNCTION eee (VAL INT x) IS x:
INT, INT, INT FUNCTION fff (VAL INT x) IS x, x, x:

REC PROC ggg ()
  SKIP
:
RECURSIVE PROC hhh ()
  SKIP
:
INLINE RECURSIVE PROC iii ()
  SKIP
:
PROC jjj ()
  SKIP
:

CHAN TYPE kkk
  MOBILE RECORD
    CHAN INT x?:
:
REC CHAN TYPE lll
  MOBILE RECORD
    CHAN INT x:
:
RECURSIVE CHAN TYPE mmm
  MOBILE RECORD
    CHAN INT x:
:

PROTOCOL nnn IS INT; INT:
PROTOCOL ooo
  CASE
    tag; INT; INT
:
