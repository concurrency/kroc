Import('env')
local = env.Clone()

local.Subst('defaults.scm.in', 
            SUBST=dict(skroc_default_libs = '/dev/null',
                       prefix = '/home/foo'))

local.Mzc('skroc', 'skroc.ss')

env['skroc'] = 'tools/skroc/skroc' 
