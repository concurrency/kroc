import scheme_utils, build_utils

Import('env')
local = env.Clone()

local.Subst('defaults.scm.in', 
              SUBST=dict(skroc_default_libs = '/dev/null',
                         prefix = '/home/foo'))

scheme_utils.BuildSchemeApp(env, 'skroc', 'ss') 


env['skroc'] = 'tools/skroc/skroc' 
