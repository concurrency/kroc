--** Pulse width modulation for the ATmega1280
-- @module pwm

#IF NOT (DEFINED (PWM.MODULE))
#DEFINE PWM.MODULE

--This is a stub. It will change soon.

PROC beginAnalog (VAL INT pin)
  SKIP
:

PROC analogWrite (VAL INT pin, VAL BYTE level)
  SKIP
:

#ENDIF
