#INCLUDE "stick.occ"

PROC type.plex (CHAN JOYSTICK in.data?, CHAN BOOL dead!, CHAN INT forward!, rotate!, scale!)
  INT number, axis.value:
  BOOL pressed, int.flag:
  SEQ

  WHILE TRUE
    in.data ? CASE
      button ; number ; pressed ; init.flag
        SKIP
      axis ; number ; axis.value ; init.flag
        SKIP
:

PROC main (CHAN BYTE kyb?, scr!, err)
  CHAN JOYSTICK joystick.data:
  -- PAR
  --  read.joystick(joystick.data!)
  SKIP  
:
